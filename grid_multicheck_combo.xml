<?xml version="1.0" encoding="UTF-8"?>
<html xmlns="http://www.w3.org/1999/xhtml"
    xmlns:ev="http://www.w3.org/2001/xml-events"
    xmlns:w2="http://www.inswave.com/websquare" 
    xmlns:xf="http://www.w3.org/2002/xforms">
    <head>
        <w2:type>COMPONENT</w2:type>
        <w2:buildDate/>
        <xf:model>
            <w2:dataCollection baseNode="map">
                <!-- 1. 그리드 데이터셋(DataList) 정의 -->
                <w2:dataList baseNode="list" repeatNode="map" id="dlt_sample" saveRemovedData="true">
                    <w2:columnInfo>
                        <w2:column id="seq" name="순번" dataType="text"></w2:column>
                        <w2:column id="userName" name="담당자" dataType="text"></w2:column>
                        <w2:column id="taskCodes" name="담당 업무" dataType="text"></w2:column>
                        <w2:column id="remark" name="비고" dataType="text"></w2:column>
                    </w2:columnInfo>
                    <w2:data use="true">
                        <w2:row>
                            <seq><![CDATA[1]]></seq>
                            <userName><![CDATA[홍길동 수석]]></userName>
                            <taskCodes><![CDATA[01,03]]></taskCodes>
                            <remark><![CDATA[화면 개발]]></remark>
                        </w2:row>
                        <w2:row>
                            <seq><![CDATA[2]]></seq>
                            <userName><![CDATA[이순신 책임]]></userName>
                            <taskCodes><![CDATA[02,04,06]]></taskCodes>
                            <remark><![CDATA[아키텍처/배포]]></remark>
                        </w2:row>
                        <w2:row>
                            <seq><![CDATA[3]]></seq>
                            <userName><![CDATA[강감찬 선임]]></userName>
                            <taskCodes><![CDATA[05]]></taskCodes>
                            <remark><![CDATA[QA 총괄]]></remark>
                        </w2:row>
                        <w2:row>
                            <seq><![CDATA[4]]></seq>
                            <userName><![CDATA[유관순 연구원]]></userName>
                            <taskCodes><![CDATA[]]></taskCodes>
                            <remark><![CDATA[신규 배정 예정]]></remark>
                        </w2:row>
                    </w2:data>
                </w2:dataList>
            </w2:dataCollection>
            <w2:workflowCollection/>
        </xf:model>

        <!-- 2. 전용 CSS 스타일 가이드 -->
        <style type="text/css"><![CDATA[
            /* 그리드 셀 내부 개행(<br/>) 및 멀티라인 높이 유연화 */
            .w2grid .cell-multiline,
            .w2grid .cell-multiline .w2grid_cell {
                white-space: normal !important;
                word-break: break-all !important;
                line-height: 1.6 !important;
                height: auto !important;
                vertical-align: middle !important;
                padding: 6px 8px !important;
            }

            /* 플로팅 다중 체크 드롭다운 레이어 */
            .ws-multicheck-layer {
                position: fixed;
                z-index: 99999;
                width: 260px;
                background-color: #ffffff;
                border: 1px solid #2563eb;
                border-radius: 6px;
                box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.2), 0 8px 10px -6px rgba(0, 0, 0, 0.1);
                font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Malgun Gothic", sans-serif;
                font-size: 12px;
                color: #1e293b;
                box-sizing: border-box;
                display: none;
            }

            /* 상단 검색 영역 */
            .ws-multicheck-header {
                padding: 8px;
                border-bottom: 1px solid #e2e8f0;
                background: #f8fafc;
            }
            .ws-multicheck-search {
                width: 100%;
                padding: 6px 10px;
                border: 1px solid #cbd5e1;
                border-radius: 4px;
                outline: none;
                box-sizing: border-box;
                font-size: 12px;
            }
            .ws-multicheck-search:focus {
                border-color: #3b82f6;
                box-shadow: 0 0 0 2px rgba(59, 130, 246, 0.15);
            }

            /* 체크박스 스크롤 목록 */
            .ws-multicheck-body {
                max-height: 180px;
                overflow-y: auto;
                padding: 6px 0;
            }
            .ws-multicheck-item {
                display: flex;
                align-items: center;
                padding: 6px 12px;
                cursor: pointer;
                transition: background 0.15s;
                user-select: none;
            }
            .ws-multicheck-item:hover {
                background-color: #f1f5f9;
            }
            .ws-multicheck-item input[type="checkbox"] {
                margin: 0 8px 0 0;
                cursor: pointer;
            }

            /* 하단 액션 버튼 */
            .ws-multicheck-footer {
                display: flex;
                justify-content: flex-end;
                gap: 6px;
                padding: 8px 10px;
                border-top: 1px solid #e2e8f0;
                background-color: #f8fafc;
            }
            .ws-btn-apply {
                background: #2563eb;
                color: #ffffff;
                border: none;
                padding: 5px 12px;
                border-radius: 4px;
                cursor: pointer;
                font-size: 12px;
                font-weight: 500;
            }
            .ws-btn-close {
                background: #e2e8f0;
                color: #475569;
                border: none;
                padding: 5px 12px;
                border-radius: 4px;
                cursor: pointer;
                font-size: 12px;
            }
            .ws-btn-apply:hover { background: #1d4ed8; }
            .ws-btn-close:hover { background: #cbd5e1; }
        ]]></style>

        <!-- 3. WebSquare scwin 자바스크립트 소스 코드 -->
        <script type="text/javascript" lazy="false"><![CDATA[
        /**
         * @target WebSquare5 Controller Script
         */
        scwin.onpageload = function() {
            // [요구사항 2] 공통 코드 매핑 객체 초기화 (코드: 라벨)
            scwin.itemCodeMap = {
                "01": "기획/전략 수립",
                "02": "화면 설계 및 기획",
                "03": "UI/UX 컴포넌트 개발",
                "04": "백엔드 API 연동",
                "05": "품질 테스트 및 QA",
                "06": "서버 배포 및 모니터링",
                "07": "사용자 매뉴얼 작성"
            };

            // 외부 클릭 시 플로팅 레이어 닫기 이벤트 등록
            document.addEventListener("mousedown", scwin.fn_handleOutsideClick);
        };

        scwin.onpageunload = function() {
            // 페이지 언로드 시 이벤트 및 동적 DOM 메모리 해제
            document.removeEventListener("mousedown", scwin.fn_handleOutsideClick);
            var el = document.getElementById("ws_floating_multicheck");
            if (el && el.parentNode) {
                el.parentNode.removeChild(el);
            }
        };

        /**
         * [요구사항 2] customFormatter: 줄바꿈 포맷팅 함수
         * "01,02,05" 형태의 코드 문자열을 라벨로 변환 후 <br/> 태그로 연결하여 반환
         */
        scwin.fn_formatMultiCheck = function(data, formattedData, rowIndex, colIndex) {
            if (!data || typeof data !== "string" || data.trim() === "") {
                return '<span style="color:#94a3b8; font-style:italic;">(선택 없음)</span>';
            }

            var codeList = data.split(",");
            var labelList = [];

            for (var i = 0; i < codeList.length; i++) {
                var code = codeList[i].trim();
                if (code !== "") {
                    // 코드 매핑 객체에서 라벨 탐색 (매핑 없으면 코드 원본 노출)
                    var label = scwin.itemCodeMap[code] || code;
                    labelList.push(label);
                }
            }

            if (labelList.length === 0) {
                return '<span style="color:#94a3b8; font-style:italic;">(선택 없음)</span>';
            }

            // HTML 태그 <br/>로 결합하여 셀 내부 개행 유도
            return labelList.join("<br/>");
        };

        /**
         * [요구사항 3] GridView 셀 클릭 이벤트
         * 'taskCodes' 컬럼 클릭 시 플로팅 다중 체크 레이어 호출
         */
        scwin.grd_main_oncellclick = function(row, col) {
            var colId = grd_main.getColumnID(col);

            // 담당 업무(taskCodes) 컬럼 클릭 시만 드롭다운 팝업 실행
            if (colId !== "taskCodes") {
                return;
            }

            // 웹스퀘어 getCellDom API로 클릭한 셀 DOM Element 획득
            var cellDom = grd_main.getCellDom(row, col);
            if (!cellDom) return;

            // 현재 셀의 선택된 코드 목록 추출
            var rawValue = dlt_sample.getCellData(row, colId) || "";
            var selectedCodes = rawValue ? rawValue.split(",").map(function(c) { return c.trim(); }) : [];

            // 플로팅 레이어 오픈
            scwin.fn_openMultiCheckLayer(cellDom, row, colId, selectedCodes);
        };

        /**
         * 플로팅 검색/체크 레이어 생성 및 좌표 배치 함수
         */
        scwin.fn_openMultiCheckLayer = function(cellDom, targetRow, colId, selectedCodes) {
            var layer = document.getElementById("ws_floating_multicheck");
            if (!layer) {
                layer = document.createElement("div");
                layer.id = "ws_floating_multicheck";
                layer.className = "ws-multicheck-layer";
                document.body.appendChild(layer);
            }

            // 현재 작업 대상 행 및 컬럼 정보 캐싱
            layer.dataset.targetRow = targetRow;
            layer.dataset.targetColId = colId;

            // getBoundingClientRect()를 활용한 셀 위치 계산
            var rect = cellDom.getBoundingClientRect();
            var topPos = rect.bottom + window.scrollY;
            var leftPos = rect.left + window.scrollX;

            // 화면 하단 넘침 방지 보정 (레이어 높이 약 280px 기준)
            if (rect.bottom + 280 > window.innerHeight) {
                topPos = Math.max(rect.top + window.scrollY - 280, 10);
            }

            layer.style.top = topPos + "px";
            layer.style.left = leftPos + "px";
            layer.style.width = Math.max(rect.width, 260) + "px";

            // 동적 레이어 템플릿 마크업 주입
            layer.innerHTML = 
                '<div class="ws-multicheck-header">' +
                    '<input type="text" id="ws_multicheck_input" class="ws-multicheck-search" placeholder="업무명 또는 코드 검색..." autocomplete="off" />' +
                '</div>' +
                '<div class="ws-multicheck-body" id="ws_multicheck_list"></div>' +
                '<div class="ws-multicheck-footer">' +
                    '<button type="button" class="ws-btn-apply" id="ws_btn_apply">적용</button>' +
                    '<button type="button" class="ws-btn-close" id="ws_btn_close">닫기</button>' +
                '</div>';

            layer.style.display = "block";

            // 체크박스 항목 리스트 렌더링
            scwin.fn_renderCheckList("", selectedCodes);

            // 검색 Input 자동 포커스 및 실시간 필터링 이벤트 등록
            var searchInput = document.getElementById("ws_multicheck_input");
            searchInput.focus();
            searchInput.addEventListener("input", function(e) {
                var keyword = e.target.value;
                // 검색 중에도 직전 체크 상태 유지
                var currentChecked = scwin.fn_getCurrentlyCheckedCodes();
                scwin.fn_renderCheckList(keyword, currentChecked);
            });

            // [적용] 버튼 클릭 시
            document.getElementById("ws_btn_apply").onclick = function() {
                var finalCodes = scwin.fn_getCurrentlyCheckedCodes();
                var combinedValue = finalCodes.join(",");

                // WebSquare DataList 데이터 갱신 -> customFormatter 자동 재호출되어 화면 갱신
                dlt_sample.setCellData(targetRow, colId, combinedValue);
                scwin.fn_closeMultiCheckLayer();
            };

            // [닫기] 버튼 클릭 시
            document.getElementById("ws_btn_close").onclick = function() {
                scwin.fn_closeMultiCheckLayer();
            };
        };

        /**
         * 검색 키워드에 따라 체크박스 목록 실시간 필터링 렌더링
         */
        scwin.fn_renderCheckList = function(keyword, selectedCodes) {
            var listContainer = document.getElementById("ws_multicheck_list");
            if (!listContainer) return;

            listContainer.innerHTML = "";
            var lowerKeyword = (keyword || "").toLowerCase().trim();

            for (var code in scwin.itemCodeMap) {
                var label = scwin.itemCodeMap[code];
                
                // 검색 조건 검사: 라벨명 또는 코드에 키워드 포함 시 노출
                if (lowerKeyword === "" || label.toLowerCase().indexOf(lowerKeyword) > -1 || code.indexOf(lowerKeyword) > -1) {
                    var isChecked = selectedCodes.indexOf(code) > -1;
                    
                    var itemLabel = document.createElement("label");
                    itemLabel.className = "ws-multicheck-item";
                    itemLabel.innerHTML = 
                        '<input type="checkbox" value="' + code + '" ' + (isChecked ? 'checked' : '') + ' />' +
                        '<span>[' + code + '] ' + label + '</span>';
                        
                    listContainer.appendChild(itemLabel);
                }
            }

            if (listContainer.children.length === 0) {
                listContainer.innerHTML = '<div style="padding:15px; text-align:center; color:#94a3b8;">일치하는 항목이 없습니다.</div>';
            }
        };

        /**
         * 현재 체크된 코드 배열 수집
         */
        scwin.fn_getCurrentlyCheckedCodes = function() {
            var checkedList = [];
            var checkboxes = document.querySelectorAll("#ws_multicheck_list input[type='checkbox']:checked");
            for (var i = 0; i < checkboxes.length; i++) {
                checkedList.push(checkboxes[i].value);
            }
            return checkedList;
        };

        /**
         * 플로팅 레이어 숨김 처리
         */
        scwin.fn_closeMultiCheckLayer = function() {
            var layer = document.getElementById("ws_floating_multicheck");
            if (layer) {
                layer.style.display = "none";
            }
        };

        /**
         * 팝업 영역 바깥 클릭 시 자동 닫힘 처리
         */
        scwin.fn_handleOutsideClick = function(e) {
            var layer = document.getElementById("ws_floating_multicheck");
            if (!layer || layer.style.display !== "block") return;

            // 클릭 위치가 레이어 내부가 아니고 그리드 셀도 아닐 경우 닫기
            if (!layer.contains(e.target) && !e.target.closest(".cell-multiline")) {
                scwin.fn_closeMultiCheckLayer();
            }
        };
        ]]></script>
    </head>
    <body ev:onpageload="scwin.onpageload" ev:onpageunload="scwin.onpageunload">
        <!-- 4. [요구사항 1] GridView XML 컴포넌트 마크업 -->
        <xf:group id="grp_wrap" style="padding:20px;">
            <w2:textbox id="tbx_title" label="WebSquare5 GridView 검색형 CheckComboBox &amp; 줄바꿈 렌더링 예제" style="font-size:18px; font-weight:bold; margin-bottom:15px;"></w2:textbox>
            
            <w2:gridView id="grd_main" 
                         dataList="data:dlt_sample" 
                         autoRowHeight="all" 
                         visibleRowNum="10"
                         style="position: relative; width: 100%; min-height: 250px;" 
                         ev:oncellclick="scwin.grd_main_oncellclick">
                <w2:header id="header1" style="">
                    <w2:row id="row1" style="">
                        <w2:column id="h_seq" name="순번" width="70" inputType="text" />
                        <w2:column id="h_userName" name="담당자" width="120" inputType="text" />
                        <w2:column id="h_taskCodes" name="담당 업무 (다중 선택)" width="280" inputType="text" />
                        <w2:column id="h_remark" name="비고" width="180" inputType="text" />
                    </w2:row>
                </w2:header>
                <w2:gBody id="gBody1" style="">
                    <w2:row id="row2" style="">
                        <w2:column id="seq" width="70" inputType="text" style="text-align:center;" />
                        <w2:column id="userName" width="120" inputType="text" style="text-align:center;" />
                        
                        <!-- [핵심 구현 컬럼] escape="false" + customFormatter + cell-multiline class -->
                        <w2:column id="taskCodes" 
                                   name="담당 업무" 
                                   width="280" 
                                   inputType="text" 
                                   escape="false" 
                                   customFormatter="scwin.fn_formatMultiCheck" 
                                   class="cell-multiline" 
                                   style="text-align: left; cursor: pointer;" />
                                   
                        <w2:column id="remark" width="180" inputType="text" style="text-align:left;" />
                    </w2:row>
                </w2:gBody>
            </w2:gridView>
        </xf:group>
    </body>
</html>
